node_type: eagerx_bridge_openai_classic_control.simnodes/OpenAIRenderer
default:
  rate:
  inputs: [tick]
  outputs: [image]
  process: 2  # {0: NEW_PROCESS, 1: ENVIRONMENT, 2: BRIDGE, 3: EXTERNAL}
  launch_file: '$(find eagerx_core)/launch/rxnode.launch'
  shape: [500, 500]

inputs:
  tick:
    msg_type: std_msgs.msg/UInt64
    repeat: all

outputs:
  image:
    msg_type: sensor_msgs.msg/Image